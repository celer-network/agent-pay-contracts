.vscode/
.DS_Store

# Forge build artifacts
cache/
out/

# Broadcast deployment logs
broadcast/

# Local environment file
.env

deployments/

# Filled-in deploy config (template lives at `script/example_config.json`).
config.json
script/config.json

# Local-machine MCP server config (contains absolute paths)
.mcp.json

# AI assistant local docs — kept out of the public repo
AGENTS.md
CLAUDE.md
.claude

# Coverage output (forge coverage --report lcov)
lcov.info
coverage/

local